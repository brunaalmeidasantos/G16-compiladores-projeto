// Codigo C gerado a partir de Codigo Intermediario
#include <stdio.h>
#include <stdbool.h>

int main() {
	// Declaracoes de variaveis podem ser inseridas aqui no futuro.
	double count; // Declaracao implicita
	count = 0;
	double t0; // Declaracao implicita
	t0 = count + 1;
	double t1; // Declaracao implicita
	t1 = (null) ? t0;
	double t2; // Declaracao implicita
	t2 = 11 ? t1;
	printf("%f\n", count);

	return 0;
}
